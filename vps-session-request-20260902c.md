# VPS側セッションへの依頼（2026-09-02・体系的な反映確認）

これまで「1つのファイルに1つの文言が含まれているか」を個別にgrepで確認するやり方を
繰り返してきたが、確認漏れ（excel-instant-webapp-demoのindex.html）や誤判定
（csv-insight-chart-demo等のSPA構成）が起きた。今回は「今このセッションが今日
触った19個のdemo全部について、ローカルとVPSのファイルが1つ残らず一致しているか」を
機械的に確認する方式に切り替える。以下をそのままVPS側のClaude Code(SSH)セッションに
貼り付けてほしい。

---

`C:\sns-factory.var2\review-files\` 直下に、今日ローカルで修正した19個のdemoについて、
各ファイルのsha256ハッシュを記録した `local-manifest-20260902.txt` と、それを使って
VPS側と突き合わせる `verify-deploy-20260902.sh` を置いた（両方ともreview-files直下）。

これを実行して、ローカル（review-files）とVPS本番配信ファイルが1ファイルずつ完全に
一致しているかを確認してほしい。個別のgrep確認ではなく、この機械的な突き合わせの
結果だけを最終的な根拠として報告してほしい。

## 実行方法

```bash
cd /path/to/review-files
chmod +x verify-deploy-20260902.sh
REMOTE_HOST=<VPSへのsshエイリアス> REMOTE_BASE=<VPS上の配信先ベースディレクトリ、例:/opt/portfolio-demos> \
  ./verify-deploy-20260902.sh
```

（`REMOTE_HOST`・`REMOTE_BASE` は、いつも使っているデプロイ時の接続先・配信先パスに
合わせて設定してほしい。demoごとに配信先ディレクトリの位置が違う場合は、スクリプト内の
`REMOTE_BASE/$demo` の組み立て方を実際の構成に合わせて調整してから実行すること）

## 結果への対応

- 「不一致あり」と出たdemoは、review-files側の最新版を正しくビルド・配信先へコピー
  （tarでの「追加」ではなく、リモート側にしか無い古いファイルも削除する「同期」にする
  こと。今回csv-insight-chart-demo等で古いビルド成果物が残存していた原因はこれ）
- 再反映後、もう一度同じスクリプトを実行し、「全demo完全一致」になることを確認する
- 最終報告には、スクリプトの最後の出力（「結論：全demo完全一致」の行）をそのまま
  含めること。個別のgrep結果ではなく、この一括突き合わせの結果を根拠とする

## なぜこの方式に変えるか（竹村さんへの説明も兼ねて）

これまでの「特定の1文言をgrepする」方式には2つの穴があった。

1. **見るファイルを間違える**：excel-instant-webapp-demoで、今日追加した新しい
   ボタン（index.html側）を確認せず、既存の別ボタン（upload.html側）を見て
   「反映済み」と誤判定した
2. **SPA構成で本文に文言が現れない**：csv-insight-chart-demo・
   gov-spec-checklist-extractor-demoはビルド後のJSバンドルに文言があり、
   トップページのHTMLをそのまま見ただけでは「無い」ように見えた

sha256による全ファイル突き合わせなら、「どのファイルを見るべきか」を毎回
考えて特定する必要がなく、1つでも中身が違えば機械的に検出できる。今後、
review-files側でdemoを修正するたびに、この形式のマニフェストを作って
同じスクリプトで確認する運用にすれば、今回のような食い違いは構造的に
起きなくなる。
