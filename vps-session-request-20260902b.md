# VPS側セッションへの再依頼（2026-09-02・訂正版）

前回の確認で見落としがあった。以下をそのままVPS側のClaude Code(SSH)セッションに
貼り付けてほしい。

---

前回の確認で、excel-instant-webapp-demoについて「index.html自体に文言が無いのは
意図した仕様（前回調査済み）」と回答したが、これは誤りだった。今日、竹村さんの
指摘を受けて `public/index.html`（一覧画面）自体に新しく直接サンプルボタンを
追加している（`upload.html`側の既存ボタンとは別の、新規の変更）。この変更が
実際にVPSへ反映されているかを確認してほしい。

## 確認してほしいこと（今度はこれだけ、ピンポイントで）

```
curl -s https://excel-instant-webapp-demo.demos.himitsuno-heya123.com/ | grep -o "同梱サンプルExcelで試す"
```

（注意：upload.html側の既存ボタンは「同梱サンプルExcel**を**試す」で、
今回追加したのは一覧画面index.html側の「同梱サンプルExcel**で**試す」。
助詞が違う。前回はupload.htmlの方（既存・を試す）だけを確認して
「反映済み」と判断してしまっていたので、必ず上記のコマンドで
**index.html自体**・**「で試す」の方**を確認すること）

- ヒットしなければ：`review-files/excel-instant-webapp-demo/public/index.html`の
  最新版をVPSの配信先ディレクトリへ反映し、PM2を再起動したうえで、
  もう一度上記のcurlコマンドを実行し、その出力を報告に含めること
- ヒットすれば：反映済み。その出力をそのまま報告に含めること

前回同様、「反映しました」だけでなく、実行したコマンドとその出力を
必ず報告に含めてほしい。
