# VPS側セッションへの依頼（2026-09-02・pii-masking-demoの警告バナー矛盾を修正）

## 背景

竹村さんより、pii-masking-demoで「同梱サンプル文で試す」ボタンを押すと
NAME（氏名）が検知される（うち1件は位置が「0-2」という不自然な範囲）のに、
画面には「今回の入力のような任意のテキストでは氏名・住所は検知されません」
という警告バナーが表示されており、矛盾しているとの指摘があった
（スクリーンショット添付）。

## 原因（詳細は`VPS-DEPLOY-PENDING.md`項目31参照）

- サンプルボタンが読み込む文章は「任意のテキスト」ではなく、mockが
  意図的に固定の検知結果（意図的な誤検知1件を含む、設計書27 §3.5の仕様通り）
  を返すよう登録済みの既知フィクスチャそのものだった。
- 警告バナーの文言は入力元に関わらず常に同じ固定文言を表示していたため、
  サンプルボタンでの意図的な検知結果に対しても矛盾して見えていた。
- **サーバー側の検知ロジック（`mockProvider.js`・`mockScenarios.js`）は
  仕様通りであり、バグではない。** 修正は`public/app.js`のみ（表示文言の
  出し分けロジック追加）。

## 反映してほしい変更

`review-files/pii-masking-demo/`側で以下を実装・テスト済み。

- `public/app.js`のみ変更。`/api/mask`のレスポンスに`source!=='rule'`かつ
  `type`がNAME/ADDRESSの検知結果が1件でもあれば「サンプル文と完全一致した
  ため、あらかじめ用意した固定の検知結果（意図的な誤検知1件を含む）を表示
  している」旨の文言に切り替え、無ければ従来通りの文言を表示する。
- `README.md`に修正内容を追記済み。
- 既存テスト27件、変更なくPASS確認済み（サーバー側ロジック無変更のため）。

## 確認手順（必須・スクリーンショット等は不要、実際の表示文言をそのまま貼り付けてください）

1. 公開URL `https://pii-masking-demo.demos.himitsuno-heya123.com/` を開く
2. 「同梱サンプル文で試す（架空データ）」ボタンを押す
3. 結果欄の警告バナーの**表示文言をそのままコピーして報告に含める**
   → 「同梱サンプル文で試す」ボタンの文章と完全に一致したため…という趣旨の
   文言になっているはずです（「今回の入力のような任意のテキストでは氏名・
   住所は検知されません」という文言のままなら反映されていません）
4. テキストエリアに何か適当な自由文（例：「これはテストです。」）を手入力し、
   「検知・マスキング実行」を押す
5. この場合の警告バナーの**表示文言もそのままコピーして報告に含める**
   → こちらは従来通り「今回の入力のような任意のテキストでは氏名・住所は
   検知されません」という文言のままのはずです

## 重要：今回の確認方式について

WebFetchでの外部要約ではなく、VPS側で直接ブラウザ（またはcurlで`/api/mask`
を叩いて`aiProvider`・`detections`と、`public/app.js`に実際に反映された
バナー文言のロジックが一致していること）を確認したうえで、**手順3・5の
実際の文言をそのまま報告に貼り付けてください**。「反映しました」という
文章だけの報告は今回受け付けられません（これまで何度か「反映した」との
報告後に未反映が判明したため、今回から実際の出力を必ず添付いただく運用に
変更します）。
